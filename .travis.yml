language: R
cache: packages

addons:
    apt:
        packages:
            - libudunits2-dev
            - libproj-dev
            - libgeos-dev
            - libgdal-dev
            - libmagick++-dev
            - libpoppler-cpp-dev

r_packages:
      - covr

after_success: Rscript -e "covr::codecov()"

