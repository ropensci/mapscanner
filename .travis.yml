# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

addons:
    apt:
        packages:
            - libudunits2-dev
            - libproj-dev
            - libgeos-dev
            - libgdal-dev
            - libmagick++-dev
            - libpoppler-cpp-dev

language: R
cache: packages
